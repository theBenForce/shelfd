{
  "name": "@shelfd/server",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "mkdir -p dist && go build -v -o dist/shelfd ./cmd/server",
    "dev": "go run ./cmd/server",
    "test": "go test -v ./...",
    "lint": "go vet ./...",
    "clean": "rm -rf dist"
  }
}
